<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Ws42" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Ws42" %>">
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
    <style>
      body {
        padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
      }
    </style>
  </head>
  <header>
      <%= render 'layouts/navigation' %>
    </header>
  <body>

    <div class="container">
      <div class="content">
        <div class="row">
          <div class="span9">
          <% if notice %>
            <div class="notice"><%= notice %></div>
          <% elsif alert %>
            <div class="alert"><%= alert %></div>
          <% end %>
          <%= yield %>
        </div>
        <div class="span3">
          <div class="well sidebar-nav">
            <h3>Users</h3>
            <ul class="nav nav-list" id="user-list">
              <li class="nav-header">Active</li>
            </ul>
          </div>
        </div>
      </div>
    </div><!--/row-->
  </div><!--/content-->
  <div id="footer">
    <div class="container">
      <p class="muted credit">Hacked together by <a href="http://www.pogoapp.com">Pogoapp</a>. Code available <a href="https://github.com/themgt/ws42-chat">here</a> (<a href="https://github.com/DanKnox/websocket-rails-Example-Project">with much borrowed</a>)</p>
    </div>
  </div>
</div> <!-- /container -->


  </body>
</html>

